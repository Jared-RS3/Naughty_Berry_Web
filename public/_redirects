# SPA fallback — /story and /quote are rendered by the app, not files on disk.
# The functions path is listed first so it is never swallowed by the catch-all.
/api/*        /.netlify/functions/:splat   200
/*            /index.html                  200
